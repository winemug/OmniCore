<?xml version="1.0" encoding="UTF-8"?>

<Shell xmlns="http://xamarin.com/schemas/2014/forms"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:local="clr-namespace:OmniCore.Mobile.Views"
       Title="OmniCore"
       x:Class="OmniCore.Mobile.AppShell">

    <!--
        The overall app visual hierarchy is defined here, along with navigation.
    
        https://docs.microsoft.com/xamarin/xamarin-forms/app-fundamentals/shell/
    -->

    <!-- 
        When the Flyout is visible this defines the content to display in the flyout.
        FlyoutDisplayOptions="AsMultipleItems" will create a separate flyout item for each child element    
        https://docs.microsoft.com/dotnet/api/xamarin.forms.shellgroupitem.flyoutdisplayoptions?view=xamarin-forms
    -->
    <!-- <FlyoutItem Title="My Profile" Icon="icon_about.png"> -->
    <!--     <ShellContent Route="AboutPage" ContentTemplate="{DataTemplate local:AboutPage}" /> -->
    <!-- </FlyoutItem> -->
    <!-- -->
    <!-- <FlyoutItem Title="Pods" FlyoutDisplayOptions="AsSingleItem"  Icon="icon_feed.png"> -->
    <!--     <Tab Title="Pod1tab"> -->
    <!--         <ShellContent Title="Pod1" ContentTemplate="{DataTemplate local:AboutPage}"/> -->
    <!--     </Tab> -->
    <!--     <Tab Title="Pod2tab"> -->
    <!--         <ShellContent Title="Pod2" ContentTemplate="{DataTemplate local:AboutPage}"/> -->
    <!--     </Tab> -->
    <!-- </FlyoutItem> -->
    <!-- -->
    <!-- <FlyoutItem Title="Transmitters" FlyoutDisplayOptions="AsSingleItem"  Icon="icon_feed.png"> -->
    <!--     <Tab Title="T1tab"> -->
    <!--         <ShellContent Title="T1" ContentTemplate="{DataTemplate local:AboutPage}"/> -->
    <!--     </Tab> -->
    <!--     <Tab Title="T2tab"> -->
    <!--         <ShellContent Title="T2" ContentTemplate="{DataTemplate local:AboutPage}"/> -->
    <!--     </Tab> -->
    <!-- </FlyoutItem> -->

    <!-- When the Flyout is visible this will be a menu item you can tie a click behavior to  -->
    <!-- <MenuItem Text="Switch Profile" StyleClass="MenuItemLayoutStyle" /> -->

    <!--
        TabBar lets you define content that won't show up in a flyout menu. When this content is active
        the flyout menu won't be available. This is useful for creating areas of the application where 
        you don't want users to be able to navigate away from. If you would like to navigate to this 
        content you can do so by calling 
        await Shell.Current.GoToAsync("//LoginPage");
    -->

    <TabBar>
        <ShellContent Route="StartPage" ContentTemplate="{DataTemplate local:StartPage}" />
        <!-- <ShellContent Route="PlatformConfigurationPage" ContentTemplate="{DataTemplate local:PlatformConfigurationPage}" /> -->
        <!-- <ShellContent Route="AccountLoginPage" ContentTemplate="{DataTemplate local:AccountLoginPage}" /> -->
        <!-- <ShellContent Route="ClientRegistrationPage" ContentTemplate="{DataTemplate local:ClientRegistrationPage}" /> -->
    </TabBar>

    <!-- Optional Templates 
    // These may be provided inline as below or as separate classes.

    // This header appears at the top of the Flyout.
    // https://docs.microsoft.com/xamarin/xamarin-forms/app-fundamentals/shell/flyout#flyout-header
    <Shell.FlyoutHeaderTemplate>
        <DataTemplate>
            <Grid>ContentHere</Grid>
        </DataTemplate>
    </Shell.FlyoutHeaderTemplate>

    // ItemTemplate is for ShellItems as displayed in a Flyout
    // https://docs.microsoft.com/xamarin/xamarin-forms/app-fundamentals/shell/flyout#define-flyoutitem-appearance
    <Shell.ItemTemplate>
        <DataTemplate>
            <ContentView>
                Bindable Properties: Title, Icon
            </ContentView>
        </DataTemplate>
    </Shell.ItemTemplate>

    // MenuItemTemplate is for MenuItems as displayed in a Flyout
    // https://docs.microsoft.com/xamarin/xamarin-forms/app-fundamentals/shell/flyout#define-menuitem-appearance
    <Shell.MenuItemTemplate>
        <DataTemplate>
            <ContentView>
                Bindable Properties: Text, Icon
            </ContentView>
        </DataTemplate>
    </Shell.MenuItemTemplate>

    -->

</Shell>