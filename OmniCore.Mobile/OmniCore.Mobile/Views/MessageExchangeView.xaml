<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="OmniCore.Mobile.Views.MessageExchangeView"
             BackgroundColor="LightYellow"
             HorizontalOptions="FillAndExpand"
             VerticalOptions="Start">
  <ContentView.Content>
      <StackLayout>
            <!--<StackLayout Orientation="Horizontal" HorizontalOptions="FillAndExpand">
                <Image Source="ic_android_phone.xml" Scale="0.2"/>
                <Image Source="ic_rl433.xml"/>
                <Image Source="ic_pod.xml"/>
            </StackLayout>-->
            <Label HorizontalTextAlignment="Center"
                   FontSize="Large" Text="{Binding ExchangeProgress.CommandText}"/>
            <Label HorizontalTextAlignment="Center"
                   FontSize="Medium" Text="{Binding ExchangeProgress.ActionText}"/>
            <Label FontSize="Small" Text="{Binding ExchangeProgress.ActionStatusText}"/>
            <Button Text="Cancel" IsEnabled="{Binding ExchangeProgress.CanBeCanceled}"/>
        </StackLayout>
  </ContentView.Content>
</ContentView>