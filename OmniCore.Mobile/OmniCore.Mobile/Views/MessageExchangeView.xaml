<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="OmniCore.Mobile.Views.MessageExchangeView">
  <ContentView.Content>
      <StackLayout>
            <!--<StackLayout Orientation="Horizontal" HorizontalOptions="FillAndExpand">
                <Image Source="ic_android_phone.xml" Scale="0.2"/>
                <Image Source="ic_rl433.xml"/>
                <Image Source="ic_pod.xml"/>
            </StackLayout>-->
            <Label HorizontalTextAlignment="Center"
                   FontSize="Large" Text="{Binding CommandText}"/>
            <StackLayout Orientation="Horizontal">
                <Label Text="{Binding ActionText}"/>
            </StackLayout>
            <Button Text="Cancel" IsEnabled="{Binding CanBeCanceled}"/>
        </StackLayout>
  </ContentView.Content>
</ContentView>