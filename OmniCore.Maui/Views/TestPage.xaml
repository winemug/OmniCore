<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="OmniCore.Maui.Views.TestPage">
    <ContentPage.Content>
        <StackLayout Orientation="Vertical" Padding="15" Margin="30" Spacing="30">
            <Button Text="Stop" Command="{Binding StopCommand}" WidthRequest="100" HorizontalOptions="Center"/>
            <Button Text="New" Command="{Binding NewPodCommand}"  WidthRequest="100" HorizontalOptions="Center"/>
            <Button Text="Prime" Command="{Binding PrimeCommand}" WidthRequest="100" HorizontalOptions="Center"/>
            <Button Text="Start" Command="{Binding StartCommand}" WidthRequest="100" HorizontalOptions="Center"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>