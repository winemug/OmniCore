<?xml version="1.0" encoding="utf-8"?>

<forms:ContentPage xmlns:forms="http://xamarin.com/schemas/2014/forms">

</forms:ContentPage>
<!--<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="OmniCore.Client.Views.PodDebugPage"
             Appearing="ContentPage_Appearing">
    <ContentPage.Content>
        <CollectionView x:Name="RadioCollection" ItemsSource="{Binding Pods}" SelectionMode="Multiple" SelectionChanged="CollectionView_SelectionChanged">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>
                    <Label Text="{Binding Lot}" Grid.Column="0"/>
                    <Label Text="{Binding Serial}" Grid.Column="1"/>
                    <Label Text="{Binding RadioAddress}" Grid.Column="2"/>
                    </Grid>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </ContentPage.Content>
</ContentPage>-->