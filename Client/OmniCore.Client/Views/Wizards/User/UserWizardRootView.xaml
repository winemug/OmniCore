<?xml version="1.0" encoding="utf-8"?>

<pages:PopupPage xmlns="http://xamarin.com/schemas/2014/forms"
                 xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                 xmlns:pages="clr-namespace:Rg.Plugins.Popup.Pages;assembly=Rg.Plugins.Popup"
                 x:Class="OmniCore.Client.Views.Wizards.NewUser.UserWizardRootView">
    <StackLayout VerticalOptions="FillAndExpand" HorizontalOptions="FillAndExpand" Padding="5"
                 Opacity="1" BackgroundColor="White">
        <StackLayout Orientation="Horizontal">
            <Switch IsToggled="{Binding OptionAddLocalUserProfile}"/>
            <Label Text="Create and manage a user profile on this device"/>
        </StackLayout>
        <StackLayout Orientation="Horizontal">
            <Switch IsToggled="{Binding OptionAddRemoteUserProfile}" IsEnabled="False"/>
            <Label Text="Manage or monitor a user profile on another device"/>
        </StackLayout>
        <Button Text="Continue" Command="{Binding SelectUserProfileTypeCommand}"/>
        <Button Text="Exit" Command="{Binding DialogCancelCommand}"/>
    </StackLayout>
</pages:PopupPage>