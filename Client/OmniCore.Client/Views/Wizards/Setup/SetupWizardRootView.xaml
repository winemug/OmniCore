<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="OmniCore.Client.Views.Wizards.SetupWizard.SetupWizardRootView">
    <ContentPage.Content>
        <StackLayout>
            <Label Text="Welcome to OmniCore."/>
            <Label Text="This software is currently in development. Would that be a problem?"/>
            <StackLayout Orientation="Horizontal">
                <Switch IsToggled="{Binding IsMaybe}"/>
                <Label Text="Maybe"/>
            </StackLayout>
            <StackLayout Orientation="Horizontal">
                <Switch IsToggled="{Binding IsPossibly}"/>
                <Label Text="Possibly"/>
            </StackLayout>
            <StackLayout Orientation="Horizontal">
                <Switch IsToggled="{Binding IsUnlikely}"/>
                <Label Text="Unlikely"/>
            </StackLayout>
            <Button Text="Continue" Command="{Binding ContinueCommand}" IsEnabled="{Binding ContinueEnabled}"/>
            <Button Text="Exit" Command="{Binding ExitCommand}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>